"""Thin setup.py retained for editable installs (`pip install -e .`) on
tooling that doesn't yet read pyproject.toml-only projects. All real
metadata lives in pyproject.toml."""
from setuptools import setup

setup()
